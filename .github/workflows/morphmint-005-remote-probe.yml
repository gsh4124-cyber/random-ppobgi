name: MorphMint 005 Remote Probe

on:
  pull_request:
    branches: [main]
    paths:
      - "global_shorts_runner/morphmint_005_remote_probe.py"
      - ".github/workflows/morphmint-005-remote-probe.yml"
      - "morphmint_005_probe_trigger.txt"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install Blender runtime libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libgl1 libxrender1 libxi6 libxkbcommon0 libxfixes3 libxxf86vm1
      - name: Download Blender 5.2.2
        run: |
          mkdir -p .cache/blender
          curl -L --fail --retry 3 -o .cache/blender/blender.tar.xz https://download.blender.org/release/Blender5.2/blender-5.2.2-linux-x64.tar.xz
          tar -xf .cache/blender/blender.tar.xz -C .cache/blender
      - name: Render MorphMint 005 three-state probe
        env:
          EGL_PLATFORM: surfaceless
        run: |
          .cache/blender/blender-5.2.2-linux-x64/blender --background --factory-startup --python global_shorts_runner/morphmint_005_remote_probe.py
      - name: Verify
        run: |
          test -s render_output/morphmint_005_probe/ceramic.png
          test -s render_output/morphmint_005_probe/chrome.png
          test -s render_output/morphmint_005_probe/crystal.png
          grep -q "MORPHMINT_005_PUBLIC_REMOTE_3STATE_PASS" render_output/morphmint_005_probe/result.json
          echo "MORPHMINT_005_PUBLIC_REMOTE_3STATE_PASS"
