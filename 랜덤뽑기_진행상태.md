---
type: artifact
status: confirmed
source_role: jeong
updated: 2026-09-03
verification: exact_cloudflare_deployment_production_browser_pass
---

# 랜덤뽑기 진행상태

장기 사업상 현재 상태를 압축한다. 실제 코드·기술 상태는 `gsh4124-cyber/random-ppobgi` 최신 `main`과 `PROJECT_STATUS.md`를 먼저 보되, **황제가 그 이후 실제 공개화면에서 확인한 회귀가 있으면 그 실사용 결과가 이전 PASS보다 우선한다.**

## 황제 확정 방향

- 한국어 본체와 해외 축소판을 따로 운영하지 않는다.
- 한국어 포함 17개 언어가 동일한 8종 랜덤뽑기 + 5종 게임도구 완제품을 사용하고 언어만 현지화한다.
- 언어별 기능코드 복제를 최소화하고 하나의 코어를 유지한다.
- 현지화 적용과 원어민 자연스러움 검증을 구분한다.
- 검색엔진 제출·크롤링·색인·노출·실제 유입·시장 성공을 구분한다.
- 광고형 페이지의 주요 수익화 Gate로 AdSense 승인 가능성을 검증한다.

## 현재 공개·QA 상태 — 2026-09-03 최신

황제가 실제 해외 공개화면에서 언어 선택기 중복·소실, 일본어/영어 화면의 한국어 잔존, 헤더 위치 회귀를 발견했고 관련 코드 수정이 진행됐다.

이후 로컬 QA만 다시 통과시키는 데서 끝내지 않고 **실제 Cloudflare Pages 공개 URL을 직접 대상으로 하는 `Production Browser Smoke`**를 별도 운영한다.

### 알려진 공개 회귀 재검증

Production Browser Smoke는 다음을 자동 확인한다.
- 16개 해외 언어 초기화면 전체 스캔
- 언어 선택기 1개 / 지구 아이콘 1개 / 현재 언어 일치
- 8종 뽑기 + 5종 게임도구 존재
- 초기·동적 UI와 접근성 속성의 한국어 잔존 검사
- `en / ja / es / zh / pt / ar`의 390px·360px 실제 상호작용
- 한국어 390px·360px 헤더·기능 검사
- 모바일 overflow, 중복 선택기, pageerror 검사

따라서 이번에 알려진 공개 회귀 유형은 현재 Production 자동 브라우저 검증 기준으로 닫혔다.

### 오래된 Cloudflare 배포를 현재 PASS로 착각하는 경로 제거

기존 Production Browser Smoke는 공개 URL이 열리면 테스트를 시작했으므로, 새 commit의 Cloudflare Pages 배포가 아직 끝나지 않은 순간에는 이전 배포본을 새 commit의 PASS로 검사할 가능성이 있었다.

Cloudflare GitHub App이 commit마다 `Cloudflare Pages` check run을 생성하고 실제 배포 commit·상태를 기록하는 것을 확인했다. 현재 push 기반 Production QA는:

1. 현재 `GITHUB_SHA`의 GitHub check-runs를 조회한다.
2. `cloudflare-workers-and-pages` 앱의 `Cloudflare Pages` check를 기다린다.
3. **현재 commit의 Cloudflare Pages check가 `completed + success`가 된 뒤에만** 공개 QA를 시작한다.
4. 그 뒤 `https://random-ppobgi.pages.dev` production alias에서 다국어 browser smoke 전체를 실행한다.

최종 확인:
- commit `f85db130468bb3788f43bddc3a6d91751023af1d`
- 해당 commit의 Cloudflare Pages deployment check: **SUCCESS**
- Production Browser Smoke run `33753483863`: **SUCCESS**
- exact commit Cloudflare deployment wait: SUCCESS
- public production alias wait: SUCCESS
- production multilingual browser QA: SUCCESS

현재 판정:
- 17개 언어 공통 코어: `IMPLEMENTED`
- 정적/로컬 브라우저 QA: `PASS`
- Cloudflare 현재 commit 배포확인: `PASS`
- 실제 공개 Production Browser QA: `PASS`
- 이전 언어 선택기·한국어 잔존 회귀: `CLOSED_BY_PRODUCTION_RETEST`
- 원어민 자연스러움·인간 실기기 체감: `NOT_CLAIMED / 별도 현실검증`

`코드 수정 완료 ≠ CI PASS ≠ Cloudflare 배포 완료 ≠ 공개 Production QA PASS`를 계속 구분한다.

## 검색 유통

- 운영 URL: `https://random-ppobgi.pages.dev/`
- `robots.txt` + `sitemap.xml` 유지
- Google Search Console 소유확인 완료
- Google 루트 URL 색인 확인
- IndexNow 자동 제출 활성
- Naver/Daum/Bing Webmaster 등 수동 등록은 [[검색엔진_등록_상태]]를 원본으로 본다.
- 실제 언어별 색인·검색노출·유입은 별도 관찰 대상이다.

## 사용 데이터

Cloudflare D1의 익명 집계 snapshot을 운영관제에 연결한다. 이는 사용자 식별이나 고유방문자 수가 아니라 제품 행동 이벤트 집계다.

## AdSense

현재 상업적 다음 Gate:

`ADSENSE_PREP / ADDRESSABILITY_UNVERIFIED`

공개 UI 회귀 Gate와 배포 race는 자동 Production QA로 닫혔으므로 다음 우선 확인은:

1. `random-ppobgi.pages.dev`를 실제 AdSense 계정 `Sites → New site`에서 추가할 수 있는지 확인
2. 승인 신청 전 소개·사용방법·개인정보·약관·문의와 공개 제품 최종 일치 확인
3. 승인 이후에만 광고 코드·개인정보 고지·ads.txt·배치 QA 진행

AdSense 사이트 등록 성공 ≠ 승인 ≠ 광고 게시 ≠ 실제 수익을 계속 구분한다.

## 다음 행동

1. Production Browser Smoke를 운영 회귀감시로 유지한다.
2. AdSense 사이트 주소 등록 가능성을 실제 계정에서 확인한다.
3. [[검색엔진_등록_상태]]의 남은 외부 검색엔진 Gate를 실행환경이 허용하는 범위에서 이어간다.
4. 실제 색인·검색유입·행동데이터를 누적해 생존 판단에 사용한다.

현재 상태 표현:

`IMPLEMENTED / LOCAL_QA_PASS / EXACT_CLOUDFLARE_DEPLOYMENT_CONFIRMED / PRODUCTION_BROWSER_QA_PASS / SEARCH_DISTRIBUTION_ACTIVE / ADSENSE_PREP`
